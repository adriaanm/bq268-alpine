# Firmware blobs (extracted from EDL dumps, not committed)
firmware/

# Build output
out/
*.img

# Compiled tools (rebuild with: just build-tools)
tools/reboot-bootloader
tools/rmt_storage
tools/libqipcrtr4msmipc.so
tools/libqmi/

# Local WiFi config (contains passwords)
wifi.conf

# Editor / IDE
.vscode/
*.swp
*~

# OS
.DS_Store

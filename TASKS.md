# Tasks

## Active

- [x] Modem data path — **PPP over SMD working.** `pppd call cellular` establishes data over UMTS. Ping 8.8.8.8 verified. BAM DMUX is unused on this firmware. See `docs/modem_data.md`.
- [ ] Bluetooth — WCNSS firmware loads (WiFi works), BT untested. btqcomsmd + BlueZ should work.
- [ ] fbcon/display stability — rapid redraws on VT2 (dmesg scroll viewer) cause kernel crash. Likely fbcon or SPI display driver issue with the ST7735S 27 Hz panel. May need rate-limiting in userspace or a kernel fix.
- [x] Power saving (screen off) — SW_LID switches CPU governor to powersave + fb0 blank. CPU hotplug crashes on re-online (CAF 4.4 SPM bug), suspend-to-RAM unlikely to work for same reason. powersave governor is the safe path.
- [ ] Battery OCV table — Current table is estimated. Calibrate with real discharge measurements.
- [ ] Battery stats daemon — **Superseded** by the metrics sampler task below; delete once `wata-metricsd` v1 lands.
- [ ] Metrics sampler (`wata-metricsd`) — Zig 0.16-dev daemon that ingests a heartbeat from wata on `/run/wata.tick`, samples battery/backlight/wifi/cellular sysfs, and writes JSONL to `/var/log/metrics/`. Aligns wakeups with wata's matrix long-poll so we don't introduce a new tick source on a device that can't suspend. Planning doc: [docs/planning/metrics-sampler.md](docs/planning/metrics-sampler.md) — treat as working document, becomes reference once implemented. Paired spec for wata: `~/wata/docs/planning/metrics-heartbeat-tick.md`. Subtasks:
  - [x] Scaffold project, protocol module with Tick datagram + tests
  - [x] Sources module: battery int fields, net operstate + counters, backlight brightness
  - [x] Sources: `batt_status` inline string reader (`[16]u8` + len, no allocator) and backlight auto-discovery via `getdents64` on `/sys/class/backlight/`
  - [x] JSONL formatter: pure `format(out, Record)` with tests for mandatory + optional fields
  - [x] Sink module: file-level open/append + size-based rotation with startup rotation for boot-cycle boundaries
  - [x] Event loop in main.zig: SOCK_DGRAM bind on `/run/wata.tick`, `timerfd` 30s watchdog, `poll()` on both, drain + sample + append
  - [x] Host-side smoke test (`just smoke-wata-metricsd`) — builds native, runs daemon on a temp dir, sends 3 ticks via `scripts/send-tick.py`, asserts JSONL output. End-to-end loop validated without root or device.
  - [x] Clean shutdown: SIGTERM/SIGINT routed through `signalfd` so OpenRC `stop` exits 0 cleanly; `--max-iters=N` flag bounds the loop for tests (smoke test now uses `--max-iters=3` instead of racing `kill`)
  - [x] Cross-compile with `-Doptimize=ReleaseSmall`: 65 KB stripped ARM binary instead of ~11 MB ReleaseSafe-with-debug. Validated burst coalescing too (10 back-to-back ticks → 2 samples with `ticks_coalesced` 4+6, all `seq`s accounted for).
  - [x] On-device smoke test: scp + `send-tick.py` against the live BQ268, JSONL populated with real battery/wlan/backlight values. Two path corrections fell out of this: backlight is `/sys/class/leds/lcd-bl/brightness` (no `backlight/` class device), and cellular is `ppp0` not rmnet (PPP over SMD per `docs/modem_data.md`). Renamed fields `rmnet_*` → `cell_*`.
  - [x] On-device cellular validation — **done in session 8** once LTE attach was unblocked. `wata-metricsd` run against a live `ppp0` on Sunrise LTE showed `cell_up=true` and `cell_rx`/`cell_tx` populating and growing (1740→2044 bytes over two samples during a ping -I ppp0 test). Fix: `readOperstate` now falls back to `/sys/class/net/ppp0/carrier` when operstate is `unknown`, because `ppp_generic.c` never calls `netif_carrier_on/off` so operstate is permanently `unknown` on PPP interfaces — the old code silently reported `cell_up=false` even with a fully-established link. Two new fixture tests cover `unknown+carrier=1 → true` and `unknown+carrier=0 → false`.
  - [x] OpenRC service `rootfs/files/etc/init.d/wata-metricsd`
  - [x] Wire into rootfs build: `rootfs/16-wata-metricsd.sh` installs the binary + service, and `just build-tools` now depends on `build-wata-metricsd` so a full rebuild picks it up automatically
  - [x] ~~Verify `/sys/class/net/rmnet_data0` is the right iface name on device~~ — resolved: no rmnet on this hardware, cellular is PPP over SMD (`ppp0`). `cell_iface` default is `ppp0`; planning doc + code + schema updated.
  - [x] Fixture-based integration tests for `sources.zig` (3 tests under `/tmp`, exercising the full BQ268-shaped sysfs tree, backlight auto-discovery, and `bl=0 → screen_on=false`). Caught and fixed a latent alignment bug in `findFirstBacklight`'s `getdents64` buffer.
- [ ] Dedicated `wata` user — wata currently runs as root via system-menu → `/opt/wata/start.sh`. Create an unprivileged `wata` user in the rootfs build and drop privs before `exec`-ing `wata-fb`. Dependency audit for group membership:
  - `video` — `/dev/fb0` (framebuffer writes)
  - `input` — `/dev/input/event*` (keypad + PTT)
  - `audio` — `/dev/snd/*` (ALSA capture/playback)
  - fbcon unbind (`/sys/class/vtconsole/vtcon1/bind`) stays at system-menu, which remains root and hands off to wata after unbinding — wata itself does not need that privilege
  - `/run/wata.tick` is `0662`, so any uid can write the heartbeat
  - Verify before dropping privs: backlight, governor switching, and SW_LID handling all live in keyd/system-menu, not wata
  - Home dir: `/var/lib/wata` (matrix state, tokens), owned `wata:wata`, mode `0700`.
- [x] eSIM provisioning — **Complete on unpatched firmware.** Full pipeline: lpac → lpac-qmi-wrapper → qmi-send-apdu → QMI UIM → eUICC. Eskimo eSIM provisioned, modem registered. Works on golden modem firmware once `qmi-send-apdu` was fixed (TLV order + dynamic UIM port lookup, see commit on 2026-04-15). The ISD-R AID filter is bypassed by truncating to the 6-byte prefix `a00000055910`.
- [ ] Walkie-talkie app — The actual application. LVGL or SDL2 on fbdev, ALSA audio, Opus codec, evdev input, QMI/ModemManager for cellular.

### Cellular data hardening / optimization

Discovered while validating `wata-metricsd` cellular field reads (2026-04-13). The modem hardware and SIM are fine — Swiss LTE coverage is excellent (Swisscom/Salt/Sunrise all `available, roaming, not-forbidden` on a network scan, RSSI -77 dBm on UMTS-900) — but the modem takes minutes-to-never to attach because of how it's configured.

**Root cause**: the modem's default `system-selection-preference` has acquisition order `cdma-1x, cdma-1xevdo, gsm, umts, lte, td-scdma` — **LTE is last**. The modem finds UMTS-900 first (Salt, MCC=228 MNC=03), gets `WCDMA Status: limited` because the SIM (Singtel, MCC=525 MNC=1) isn't allowed PS attach there, and parks on it without ever trying LTE. The CLAUDE.md hardware spec says "4G LTE data-only" but the actual config doesn't enforce that.

- [x] **Force LTE-only mode at cellular bringup** — `cell-data wake` now calls `ensure_lte_prefs` which idempotently applies `lte,automatic` if not already set. Acquisition order becomes `lte, umts, gsm, …` so LTE is tried first.
- [x] **Persist mode preference across modem resets** — verified: the QMI NAS preference is stored in modem NV and survives both `dms-set-operating-mode=reset` and full device reboots. `ensure_lte_prefs` is therefore a no-op after the first application. No oneshot service needed.
- [x] **Robust modem state-machine in `cell-data.sh`** — rewritten with a bounded 2-minute budget: `set_online` handles `online`/`low-power`/`offline`/`shutting-down`/`resetting` and escalates to `dms reset` + 10 s settle once on timeout. `do_wake` returns exit 1 for unreachable-online and exit 2 for PS-attach timeout, so callers (and `smoke-cellular`) can distinguish firmware deadlock from coverage/roaming problems.
- [x] **Roaming detection and logging** — `log_serving` records `plmn`, `rat`, `roaming` bool (MCC/MNC mismatch vs `nas-get-home-network`), home PLMN, and QMI's own roaming status to `/var/log/cellular.log` on every successful attach.
- [x] **`just smoke-cellular` end-to-end recipe** — runs `cell-data wake && up && ping -I ppp0 8.8.8.8 && down` against the live device. `just push-cell-data` scp's the current script in-place for quick iteration without a rootfs rebuild.
- [x] **Document the cellular bringup chain** — `docs/modem_data.md` now has a "Bringup chain (2026-04-13)" section with the sequence, expected prefs, recovery table, and smoke-test pointer.
- [x] **Approved roaming partners list — generated from authoritative source** — the runtime file `/etc/cellular/roaming-partners` is now auto-generated by `tools/gen-roaming-partners.py` (wired as `just gen-roaming-partners`), joining `eskimo_roaming.md` (manually extracted from https://www.eskimo.travel/en/network-coverage, 2026-04-13) against `tools/data/mcc-mnc-list.json` (vendored pbakondy/mcc-mnc-list, ~3000 operators). The script fuzzy-matches brand names, follows parent-brand aliases (Telefonica→Movistar/O2/Vivo, Veon→Beeline/Kyivstar, …), and caps multi-brand operators at 4 MCC/MNC rows to keep the file small. Current resolution: 118/139 Eskimo entries → 154 MCC/MNC pairs. 21 entries unresolved (small territories + brand-name gaps) — listed in script stderr output; fix individually by extending `BRAND_PARENTS`/`COUNTRY_ALIASES` and re-running. Home SIM is Eskimo (MVNO on Singtel IMSI 525-01). **Re-run after updating `eskimo_roaming.md`** — e.g. new partner relationships, or switching from Eskimo to a different eSIM provider; the script works unchanged as long as the new markdown uses the same `- Country\n  - Operator RAT` shape.
- [x] **DIAG NAS log subscription** — `tools/cell-diag.c` opens /dev/diag, switches to MEMORY_DEVICE/MPSS mode, subscribes to LTE equipment class (equip_id 0x0B, items 0xC0..0xFF) via LOG_CONFIG SET_MASK, and appends one JSONL line per parsed log packet to `/var/log/cellular-diag.log`. Wired as `just diag-capture [duration]`. First on-device run during a full `cell-data wake` cycle (commit 0591b6d) captured 153 × 0xB0C0 LTE_RRC_OTA + 0xB0C3 serving-cell + 0xB0C4 PLMN search + 0xB0EE NAS PLMN op — but **zero 0xB0E2/0xB0E3 EMM_OTA_IN/OUT**. That's the first real datapoint in the attach-failure investigation: the modem never gets as far as sending a NAS ATTACH REQUEST, so the failure is in RRC / cell selection / radio bearer setup, not in NAS reject or HSS roaming. See the revised "Live LTE attach still failing" entry below for next steps.
- [x] **LTE-only attach block — mitigated via UMTS fallback.** Commit 7df1869 switches `cell-data` RAT preference from `lte,automatic` to `lte|umts,automatic`, fixes the `ensure_rat_prefs` grep so it stops re-applying prefs every wake, and fixes `ensure_mcfg` `-A1→-A4` so the already-active Singtel MCFG isn't re-activated every wake (which was triggering a modem reset and dropping registration mid-flow). With those three fixes the modem attaches cleanly to Orange F / Sunrise / Salt on UMTS and stays attached across repeated `cell-data wake` cycles.
- [x] **PDP-activation reject cause identified — #33, Orange F Eskimo agreement is signalling-only.** Confirmed via direct AT on `/dev/smd7` (commit 4078320): on a clean UMTS attach to Orange F (serving PLMN `Orange F Eskimo`, 208/01), `AT+CGACT=1,1` returns `+CME ERROR: requested service option not subscribed` and `AT+CEER` confirms 3GPP 24.008 cause **#33 "requested service option not subscribed"**. Tried APNs `globaldata`, `hicard`, `internet`, empty — identical reject. The Eskimo MVNO has a signalling roaming agreement with Orange France (enough for NAS attach and the "Orange F Eskimo" SPN) but **no data roaming** on that carrier. So the previously-hypothesised "account is unfunded" was wrong: the account works, the network we keep attaching to just isn't on the data-roaming allowlist for this plan. Android at the same location attaches to **Sunrise (228/02) on LTE**, which is the correct partner for Eskimo data in CH.
- [x] **`cell-diag` LTE RRC channel-type parser + LTE-only capture recipe** — `tools/cell-diag.c` now decodes the 0xB0C0 LTE_RRC_OTA header (version-dispatch: ver≤9 u16 earfcn, ver≥13 u32 earfcn), emitting `{ver, pci, earfcn, ch, ch_name}` per event and an `RRC_SUMMARY` histogram on exit. `tools/cell-data.sh` now accepts a `CELL_DATA_RAT` env override consumed by `ensure_rat_prefs`, and `just diag-capture-lte` wraps a `CELL_DATA_RAT=lte cell-data wake` inside a cell-diag capture with automatic restore of `lte|umts` afterwards. Parser validated on first on-device run (ver=13, PCIs/EARFCNs sensible, 241/241 parse success).
- [x] **Unpatched modem firmware → full Sunrise LTE attach + data on first try.** RRC_SUMMARY went from `bcch_dl_sch=241 ul_ccch=0` (patched) to `bcch_dl_sch=81 pcch=67 ul_ccch=1 dl_ccch=1 ul_dcch=49 dl_dcch=14` (unpatched) in a single 90 s capture. QMI then reported `registered / CS attached / PS attached / lte / MCC 228 MNC 2 (Sunrise)` with RSRP -111, and `cell-data up` → PPP up on ppp0 `100.100.105.20` → ping 8.8.8.8 0% loss. First real LTE data on this device.
- [x] **`cell-diag` raw payload + `decode-bcch.py` SIB1 decoder.** `tools/cell-diag.c` now dumps the full 0xB0C0 LTE_RRC_OTA payload under `"raw"` (the old best-guess pdu_off/pdu_len slice was off-by-two). `tools/decode-bcch.py` slices `raw[19:]` out of each BCCH_DL_SCH event and runs it through pycrate's `EUTRA_RRC_Definitions.BCCH_DL_SCH_Message`, emitting per-cell PLMN list / TAC / Cell ID / cellBarred / intraFreqReselection / q-RxLevMin. Used to rule out cell-level blocks in session 8. Requires `pycrate` via `pip3 install --break-system-packages pycrate`.
- [x] **`cell-diag` LTE log mask widened** to equipment-id 0x0B items 0x00..0xFF (was 0xC0..0xFF). Now also captures 0xB063 (ML1 DL bursts), 0xB0C3/0xB0C4 (serving cell / PLMN search), 0xB0CB (ML1 serving cell state on unpatched firmware), 0xB0EE (NAS PLMN op). Did not change the key UL_CCCH finding but gives much more context for future debugging.
- [x] **Fixed `tools/qmi-send-apdu.c` — lpac works on golden (unpatched) modem firmware.** 2026-04-15: the failure was a userspace wire-format bug, not a firmware problem. Two fixes in one commit: (1) emit TLVs in descending type-ID order (AID 0x10 before Slot 0x01, etc.) to match libqmi — the MSM8909 UIM service rejected ascending order with QMI error 50; (2) look up the UIM service's node/port dynamically via `IPC_ROUTER_IOCTL_LOOKUP_SERVER` instead of hardcoding port 39, because the modem reassigns `port_id` on every restart. With both fixes applied, `qmi-send-apdu test` runs the full ISD-R sequence on golden firmware, `lpac-qmi-wrapper chip info` returns EID + eUICC info, and LTE stays attached on Sunrise simultaneously. Consequence: all firmware-patching scaffolding (`tools/patch-modem-b12.py`, the `provision-esim-*` recipes, `docs/esim_provision.md`, `docs/planning/cellular-attach-resume.md`) has been deleted — golden firmware is the only mode. In-tree `firmware/modem/` stays golden.
- [x] **Pre-flight the modem in `diag-capture-lte`.** Recipe now polls `dms-get-operating-mode` up to 8 × 15 s waiting for `online`, and forces it via `dms-set-operating-mode=online` if the modem reports `offline`/`low-power`/etc. Both session 7's "device crash mid-wake" and session 8's "first attempt after reboot is a no-op" traps are handled. Verified end-to-end in session 8's bisection — 6 reboot cycles, zero manual intervention needed. Session 8 re-found a known trap: on fresh boot, `dms-get-operating-mode` returns `shutting-down` for up to ~2 minutes even though the modem peripheral's `subsys/state` is `ONLINE`, and `cell-data wake`'s 20 s `WAKE_BUDGET` can't ride it out — the reset-escalation hit during session 7's first attempt triggered a device reboot. The recipe should: poll `dms-get-operating-mode` until `online`, or issue a single `dms-set-operating-mode=online` before invoking the wake, and also offline/online-cycle any stale PS attach from a previous run so LTE-only wake doesn't silently inherit a UMTS attach.
- [x] ~~**🔔 KEY FINDING: LTE RRC Connection Request is never transmitted, on any band.**~~ — Superseded by session 8. The finding was correct for patched firmware but the root cause was upstream of RRC: NAS never asking RRC to connect because of the patch-induced kill-switch, not a NAS policy issue. Left here for audit trail. Original text: First 90 s LTE-only capture (session 7) yielded 241 BCCH_DL_SCH events across 6+ distinct cells on B1 / B3 / B7, with **zero UL_CCCH, zero DL_CCCH, zero DL_DCCH, zero PCCH**. The modem is happily decoding SIBs from multiple LTE cells and never once kicking off a random-access / RRC Connection Request procedure. **This kills the B20 UL TX hardware hypothesis** — the failure is uniform across bands, so it's not an RF-path asymmetry. The failure is upstream of RRC, in NAS PLMN-selection / MCFG-SW policy / attach-attempt-counter land. Full analysis preserved in git history.
- [ ] ~~**⏭️ NEXT: Figure out why NAS never tells RRC to start.**~~ — Session 8 answered this: patched modem firmware silently disables LTE NAS→RRC progression. Original breakdown preserved below in case any sub-items become useful after the patched/unpatched firmware mode-switch lands. The 241-to-zero DL-vs-UL ratio means we don't need to look at RF hardware or band preferences anymore. Ordered follow-ups:
  1. **Widen `cell-diag` log mask to equip_id 0x0B 0x00..0xFF** to catch any NAS state events we might be missing. Currently we subscribe 0xC0..0xFF; 0xB0E0 LTE_NAS_EMM_STATE is in that range, but zero events landed in session 7's capture — either NAS doesn't transition, or that event ID is mapped differently on this firmware. Also add equip_id 0x04 (GSM/WCDMA NAS) and 0x07 (ML1) so we can see inter-RAT reselection decisions and cell-selection Srxlev rejections.
  2. **Decode the 241 BCCH_DL_SCH payloads offline from `cell-diag-lte.log`**. Each payload is an ASN.1 RRC PDU — SIB1 gives us `cellBarred`, `intraFreqReselection`, `plmn-IdentityList`, and `cellReservedForOperatorUse` flags per cell. If any visible cell is barring our PLMN (525/01 Singtel) explicitly that would immediately explain why NAS refuses to kick RRC. Use `pycrate_asn1rt` or similar, don't reinvent.
  3. **Clean-cycle test**: fresh boot → one `qmicli --dms-set-operating-mode=online` (no `cell-data wake`, no `ensure_rat_prefs`, no MCFG activation) → `diag-capture-lte` replaced by a minimal capture that just subscribes and waits 90 s. If the first wake after a clean cycle transmits UL_CCCH and subsequent ones don't, our script's state churn (attach-attempt counter, T3346) is the culprit. If it still doesn't, the MCFG/NAS rules are.
  4. **Diff active MCFG-SW NV items against Android's**. Session 4 decoded 22 NV-by-number items in `Singtel_Commercial`; we never went back and decoded the SOR/preferred-PLMN bitmap fields. That's where a "Singtel-steering-to-Orange-FR-is-fine-but-Sunrise-and-every-other-CH-PLMN-is-blocked-for-PS" rule would live.
  5. **Known workflow trap**: do not run `just diag-capture-lte` on a freshly-booted device without first confirming `dms-get-operating-mode = online`. The session 7 first attempt crashed the device (uptime reset) because the modem was still in `shutting-down` when `cell-data wake` hammered it. Pre-flight check: `ssh bq268 'qmicli -p -d msmipc://0 --dms-get-operating-mode | grep online'`.
- [x] ~~**(deferred) Steer the modem onto Sunrise LTE — only relevant after RRC is unblocked.**~~ — Moot: with unpatched firmware the modem picks Sunrise LTE automatically in <10 s (session 8). No manual steering needed. Concretely: we have two gates to pass through at the same time, and both need code/config fixes on our side.
  1. **Don't camp on Orange F**: the modem ignores the userspace `nas-set-preferred-networks` list on UMTS even when we set Sunrise-only with access-tech `all`, because Orange F has a stronger UMTS signal at the bench and automatic reselection prefers it. We need either to (a) populate EF_FPLMN (SIM-backed forbidden-PLMN list) with `208/01` and other non-data partners so the modem refuses to camp there — requires writing the EF via UIM or an AT+CRSM command; or (b) find a qmicli/NV knob that makes the user preferred-networks list *block* non-listed PLMNs rather than just *prefer* listed ones; or (c) restrict to LTE-only, which naturally excludes Orange F on UMTS — but only works if gate 2 is also solved.
  2. **Actually complete LTE attach on Sunrise**: today's `lte,automatic` tests fall back to `not-registered-searching`, and DIAG captures show the modem reads SIBs from B20 cells (EARFCN 6400/6300) but never transmits an RRC Connection Request or NAS ATTACH REQUEST. Earlier session notes hypothesised a **B20 UL TX path problem** (RX works — scans see cells — but UL may not; the B20 PA/filter population on the BQ268 PCB is suspect). Before diving into hardware, try: (i) extend `cell-diag` to parse 0xB0C0 RRC OTA `pdu_num` so we can see whether *any* UL_CCCH (RRC Connection Request) ever leaves during a wake — that confirms or refutes the UL-broken hypothesis cheaply; (ii) if UL is OK, check whether Sunrise has a non-B20 LTE cell (EARFCN 1850 B3 or 2850 B7 per the Android reference) we can bias the modem towards; (iii) if UL on B20 is genuinely broken, the only mitigation is an LTE band restriction that excludes B20 — qmicli has no flag for this, so it would need raw QMI NAS TLV construction. The LTE-band-preference task was dropped earlier as "not worth it"; this reopens it with a concrete reason.
  3. **Once attached to Sunrise on LTE**, re-run the direct AT test (`AT+CGDCONT=1,"IP","hicard"` then `AT+CGACT=1,1`) and expect a clean `OK` with a real `CGPADDR` — that's the 2026-04-03 "PPP over SMD works" state restored. `pppd call cellular` should then complete IPCP with real DNS (165.21.x on Singtel) and ping 8.8.8.8 should flow.
  **Tools already in place**: `tools/cell-diag` (DIAG log-mask subscription, just needs RRC-pdu-type parsing); `just diag-capture [duration]`; `cell-data wake` + preferred-networks machinery (currently cosmetic on UMTS reselection); direct `/dev/smd7` AT access shown to work with `printf AT\r\n > /dev/smd7 & dd if=/dev/smd7`.
- [ ] **QMI-proxy boot-time orphan subscriptions** — libqmi commit fbd4aae + bq268-alpine commit 07921ff wire `qmi-proxy` as a persistent QMI multiplexer and switch every in-tree caller to `qmicli -p`. That halved the leak rate and eliminated per-call growth, but 4 `kworker/u8:N` still pile up in `D` state after every boot and `send_filled_buffers_to_user: Send Failed -3 drop_count=...` still grows at ~10/min, pinning load average around 4. Suspect: the modem init retry loop hammers `qmicli -p --dms-set-operating-mode=online` against the proxy while Q6 DSP isn't ready yet, and *something* in the proxy's first-open-failure path leaks an ipc_router subscription per retry. Fix: have `/etc/init.d/modem` wait for Q6 readiness via `/dev/subsys_modem` / sysfs state (not QMI) and then issue **one** blocking `dms-set-operating-mode=online` once Q6 is up. Validate by rebooting and confirming zero `DW` kworkers + `drop_count` flat. Track via task #8.
- [ ] ~~LTE band preference review~~ — dropped: `qmicli` has no flag for setting the LTE band list (only mode + network-selection), and the current default (B1/3/5/7/8/20/28 + extended B66/71) already covers EU + common travel. Would require raw QMI TLV construction, not worth it.

### Zig 0.16 tool ports

Porting C tools to Zig 0.16.0 (release, `~/zig-x86_64-linux-0.16.0/zig`). Build umbrella at `tools/build.zig`, cross-compile via `just build-zig-tools`. Patterns established in wata-metricsd: raw `linux.fd_t`, `linux.errno()` switch, `init.args.iterate()`, `posix.openatZ(AT.FDCWD, ...)`.

- [x] `reboot-bootloader` — trivial syscall, 24→25 LOC. Commit `6ac8401`.
- [x] `rmt_storage` — 1013 LOC daemon. Verified on device: cold reboot → UIO discovery → modem EFS boot → LTE attach on Sunrise. Earlier reboots were caused by `rc-service modem restart` races, not the daemon itself.
- [x] `qmi-send-apdu` — 665 LOC QMI UIM client. Verified: byte-identical wire format to C, full eUICC test pass, daemon mode works with lpac.
- [ ] `cell-diag` — 544 LOC DIAG log subscriber. Needs `std.fs.File` → raw fd rework.
- [ ] `diag-apdu` — 547 LOC. Extract shared `diag.zig` module with cell-diag.
- [ ] `diag-efs-write` — 1189 LOC. Largest DIAG tool.
- [x] `libqipcrtr4msmipc` — deleted (commit `7a77f3e`), custom libqmi has native AF_MSM_IPC.

## Backlog

- [ ] Read-only rootfs — Production hardening. Prevents eMMC wear and corruption from hard power-off. overlayfs on tmpfs for /var, /tmp.
- [ ] OTA updates — Mechanism for deploying rootfs updates over cellular/WiFi. Dual-partition (A/B) or full-image reflash.
- [ ] Watchdog timer — Hardware watchdog (QCOM WDT) to auto-reboot on hang. Critical for unattended field device.
- [ ] Unused service cleanup — Remove acpid, machine-id, watchdog services pulled in by Alpine defaults. Reduces boot noise and attack surface.
- [x] Power switch screen on/off — DTS updated to SW_LID, keyd wired to screen-on/screen-off via fb0 blank + governor switch.
- [ ] Kernel module trimming — Strip unused modules from rootfs to save space and boot time. Only load what's needed.
- [ ] Security hardening — Drop to non-root for the app. Disable root login over cellular. Firewall (nftables) to restrict outbound to app traffic only.
- [ ] Modem DIAG logging — DIAG fully functional (SMD channels open). Useful for debugging modem issues.
